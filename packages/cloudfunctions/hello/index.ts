/**
 * Placeholder CloudBase function — no business logic.
 * Compile with `pnpm --filter @peakmeet/cloudfunctions build`.
 */
export function main(): { ok: true; message: string } {
  return { ok: true, message: 'peakmeet-hello' };
}
